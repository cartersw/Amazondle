import type { Email } from '../resources/Email';
import { AbstractAPI } from './AbstractApi';
type EmailParams = {
    fromEmailName: string;
    emailAddressId: string;
    subject: string;
    body: string;
};
/**
 * @deprecated This endpoint is no longer available and the function will be removed in the next major version.
 */
export declare class EmailAPI extends AbstractAPI {
    /**
     * @deprecated This endpoint is no longer available and the function will be removed in the next major version.
     */
    createEmail(params: EmailParams): Promise<Email>;
}
export {};
//# sourceMappingURL=EmailApi.d.ts.map